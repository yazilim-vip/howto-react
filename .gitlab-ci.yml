image: node:12.18.2
  
pages:
  script:
    - npm i && npm run-script build
    - rm -rf public && mv build public
    - cp public/index.html public/404.html
  artifacts:
    paths:
      - public
  only:
    - master